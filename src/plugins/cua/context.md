CUA Driver is available for local macOS desktop automation when this plugin is enabled.

Use CUA only for user-requested visible UI tasks. Check permissions before the first action, observe the target window before each action, and verify the result with a fresh observation after each state change.
